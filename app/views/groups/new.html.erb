<h1>新規グループ作成</h1>

<%= form_with(model: @group, local: true) do |f| %>
  <%= f.label :name, 'グループ名' %>
  <%= f.text_field :name %>

  <%= f.label :purpose, 'グループの目的' %>
  <%= f.text_area :purpose %>

  <%= f.submit 'グループを作成' %>
<% end %>